import { Component, OnInit} from '@angular/core';
import { Graph } from './graph/graph';

@Component({
  imports: [ Graph],
  selector: 'app-home',
  styleUrl: './home.css',
  templateUrl: './home.html',
})
export class Home {

  
}
